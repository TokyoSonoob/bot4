{
  "name": "fb-group-poster",
  "version": "1.0.0",
  "description": "FB group poster via remote Chrome (Browserless) using only env cookies + api",
  "main": "index.js",
  "license": "ISC",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node index.js",
    "test": "echo \"no tests\" && exit 0"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "node-cron": "^4.2.1",
    "puppeteer-core": "^24.16.2",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
